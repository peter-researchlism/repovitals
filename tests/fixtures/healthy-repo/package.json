{
  "name": "healthy-fixture",
  "version": "1.0.0",
  "description": "A small healthy-looking fixture repo for RepoVitals tests.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "left-pad": "^1.3.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
