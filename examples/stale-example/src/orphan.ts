// This file is intentionally not imported anywhere → RepoVitals should flag it
// as possibly-unused (P1: dead code).

// TODO: wire this up to the CLI
export function unused(): string {
  return "I am not referenced from anywhere.";
}
